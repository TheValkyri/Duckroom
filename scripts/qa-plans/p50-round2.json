[
  { "viewport": [390, 844, 3] },
  { "navigate": "/library", "settle": 9000 },
  { "eval": "(() => { const tabs = [...document.querySelectorAll(\"nav[aria-label='Điều hướng chính'] > div > a, nav[aria-label='Điều hướng chính'] > div > button\")]; return JSON.stringify({ tabCount: tabs.length, labels: tabs.map(t => t.textContent.trim().slice(0, 10)) }); })()", "name": "nav-tabs" },
  { "eval": "(() => { const more = [...document.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('Xem thêm')); if (!more) return 'more-btn: MISSING'; more.click(); return 'more-opened'; })()", "name": "open-more" },
  { "wait": 900 },
  { "eval": "(() => { const sheet = document.querySelector('[aria-label=\"Xem thêm mục điều hướng\"]'); if (!sheet) return 'more-sheet: MISSING'; const items = [...sheet.querySelectorAll('a')].map(a => a.textContent.trim().slice(0, 20)); const r = sheet.getBoundingClientRect(); return JSON.stringify({ items, sheetTop: Math.round(r.y), coversScreen: r.y > 300 }); })()", "name": "more-sheet" },
  { "eval": "(() => { const link = [...document.querySelectorAll('[aria-label=\"Xem thêm mục điều hướng\"] a')].find(a => a.getAttribute('href') === '/singles'); link.click(); return 'navigated-to-singles'; })()", "name": "go-singles" },
  { "wait": 1500 },
  { "eval": "(() => { const path = location.pathname; const moreBtn = [...document.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('Xem thêm')); const sheetGone = !document.querySelector('[aria-label=\"Xem thêm mục điều hướng\"]'); return JSON.stringify({ path, sheetClosed: sheetGone, moreActive: moreBtn ? moreBtn.className.includes('text-primary') : false }); })()", "name": "singles-state" },
  { "tapSelector": "main .group button", "settle": 2000 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); if (!d) return 'no dock'; const btns = [...d.querySelectorAll('button')].map(b => b.getAttribute('aria-label')).filter(Boolean); return JSON.stringify({ miniButtons: btns, hasPrev: btns.includes('Bài trước'), hasNext: btns.includes('Bài sau') }); })()", "name": "mini-prev-next" },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()", "name": "expand" },
  { "wait": 1400 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); if (!fs) return 'no fs'; const minBtn = [...fs.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Thu nhỏ trình phát'); const r = minBtn.getBoundingClientRect(); const lyrBtn = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('lời bài hát')); const lyrR = lyrBtn.getBoundingClientRect(); return JSON.stringify({ minTop: Math.round(r.y), minLeft: Math.round(r.x), lyrTop: Math.round(lyrR.y), headerBreathing: r.y >= 10 }); })()", "name": "header-spacing" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('lời bài hát')).click(); return 'lyrics-open'; })()", "name": "open-lyrics" },
  { "wait": 1000 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sheet = [...fs.querySelectorAll('[role=dialog]')].find(d => (d.getAttribute('aria-label')||'').includes('Lời bài hát')); if (!sheet) return 'lyrics-sheet: MISSING'; const r = sheet.getBoundingClientRect(); const cs = getComputedStyle(sheet); const handle = sheet.querySelector('.h-1\\\\.5.w-10'); return JSON.stringify({ isBottomSheet: r.y > 250, rounded: cs.borderRadius, hasHandle: !!handle, bg: cs.backgroundColor.slice(0, 30) }); })()", "name": "lyrics-sheet-geometry" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sheet = [...fs.querySelectorAll('[role=dialog]')].find(d => (d.getAttribute('aria-label')||'').includes('Lời bài hát')); const close = [...sheet.querySelectorAll('button')].find(b => b.getAttribute('aria-label')?.includes('Đóng lời')); close.click(); return 'lyrics-closed'; })()", "name": "close-lyrics" },
  { "wait": 800 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const cur = [...fs.querySelectorAll('p')].find(p => p.className.includes('font-medium leading-6')); return cur ? 'current-line-on-player: OK — ' + JSON.stringify(cur.textContent.slice(0, 30)) : 'current-line: not found (no lyrics or idle)'; })()", "name": "current-line" },
  { "measureFPS": 2000, "name": "fullscreen-fps" }
]
