[
  { "viewport": [1440, 900, 1], "name": "desktop" },
  { "eval": "location.href = 'http://localhost:5173/library'; 'r'", "name": "reload" },
  { "wait": 22000 },
  { "eval": "(() => { const btn = [...document.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Tùy chỉnh giao diện'); btn.click(); return 'picker'; })()" },
  { "wait": 800 },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const light = [...dialog.querySelectorAll('button')].find(b => b.textContent.includes('Sáng')); light.click(); return 'ripple-start'; })()", "name": "start-ripple" },
  { "eval": "(() => { const layer = document.getElementById('duckroom-theme-ripple'); if (!layer) return 'layer-gone-fast'; const rings = layer.children.length; const cs = getComputedStyle(layer.children[0]); return JSON.stringify({ ringCount: rings, isBorderRing: cs.boxShadow.length > 30, bgSet: cs.backgroundColor, childCount: layer.children.length }); })()", "name": "ripple-shape" },
  { "eval": "(() => { const visible = [...document.querySelectorAll('main .group')].length; const bodyBg = getComputedStyle(document.body).backgroundColor; return JSON.stringify({ rowsStillVisible: visible, bodyBg }); })()", "name": "content-during-ripple" },
  { "measureFPS": 800, "name": "fps-during-ripple" },
  { "wait": 800 },
  { "eval": "(() => { return JSON.stringify({ cleaned: !document.getElementById('duckroom-theme-ripple'), mode: document.documentElement.getAttribute('data-theme') }); })()", "name": "cleaned" },
  { "screenshot": "C:/Users/Darne/AppData/Local/Temp/opencode/shots/ripple-v3.png" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const hue = dialog.querySelectorAll('input[type=range]')[0]; const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; window.__dragStart = performance.now(); for (let h = 0; h <= 360; h += 4) { setter.call(hue, String(h)); hue.dispatchEvent(new Event('input', { bubbles: true })); } return 'dragged-90-events'; })()" },
  { "measureFPS": 1200, "name": "fps-during-drag" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const hue = dialog.querySelectorAll('input[type=range]')[0]; hue.dispatchEvent(new PointerEvent('pointerup', { bubbles: true })); return 'released'; })()" },
  { "eval": "(() => { return JSON.stringify({ colorAfterDrag: document.documentElement.style.getPropertyValue('--primary').trim(), persistDebounced: JSON.parse(localStorage.getItem('duckroom.theme.v1')).hue }); })()", "name": "drag-result" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const close = [...dialog.querySelectorAll('button')].find(b => b.getAttribute('aria-label')?.includes('Đóng')); close.click(); return 'closed'; })()" },
  { "wait": 500 },
  { "eval": "(() => { return JSON.stringify({ path: location.pathname, rows: document.querySelectorAll('main .group').length, bodyBg: getComputedStyle(document.body).backgroundColor, mode: document.documentElement.getAttribute('data-theme') }); })()", "name": "light-state" },
  { "viewport": [390, 844, 3], "name": "phone" },
  { "eval": "location.href = 'http://localhost:5173/library'; 'r'", "name": "phone-reload" },
  { "wait": 22000 },
  { "tapSelector": "main .group button", "settle": 2000 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()" },
  { "wait": 1300 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const viz = fs.querySelector('canvas'); return viz ? 'visualizer present in light mode' : 'no canvas'; })()", "name": "viz-light" }
]
