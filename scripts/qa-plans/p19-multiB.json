[
  { "viewport": [390, 844, 3], "navigate": "/library", "settle": 4000 },
  { "wait": 1000 },
  {
    "eval": "(() => { const dock = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); if (!dock) return 'tabB no-dock (no track in this tab)'; const title = dock.textContent.split('\\n')[0]; const strip = dock.querySelector('.h-0\\\\.5 div'); const pause = [...dock.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Tạm dừng'); return JSON.stringify({ tabBTitle: title.slice(0, 30), progress: strip ? strip.style.width : 'none', showsPause: !!pause }); })()",
    "name": "tabB-state"
  },
  { "wait": 3000 },
  {
    "eval": "(() => { const dock = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); const strip = dock.querySelector('.h-0\\\\.5 div'); return 'tabB progress later: ' + (strip ? strip.style.width : 'none'); })()",
    "name": "tabB-progress-later"
  }
]
