[
  { "navigate": "/library", "settle": 4000 },
  {
    "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; rows[0].querySelector('button').click(); return 'tabA: queue started via DOM click'; })()",
    "name": "tabA-play-dom"
  },
  { "wait": 3000 },
  {
    "eval": "(() => { const dock = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); return JSON.stringify({ title: dock?.textContent.slice(0, 25), progress: dock?.querySelector('.h-0\\\\.5 div')?.style.width }); })()",
    "name": "tabA-dock"
  }
]
