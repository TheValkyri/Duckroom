[
  { "viewport": [390, 844, 3], "navigate": "/library", "settle": 4000 },
  { "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; rows[0].querySelector('button').click(); return 'tabA-played-row1'; })()", "name": "tabA-play" },
  { "wait": 2500 },
  {
    "eval": "(() => { const dock = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); const strip = dock.querySelector('.h-0\\\\.5 div'); return 'tabA progress: ' + (strip ? strip.style.width : 'none'); })()",
    "name": "tabA-progress"
  }
]
