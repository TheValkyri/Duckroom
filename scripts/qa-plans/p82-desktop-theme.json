[
  { "viewport": [1440, 900, 1], "name": "desktop" },
  { "eval": "location.href = 'http://localhost:5173/library'; 'r'", "name": "reload" },
  { "wait": 20000 },
  { "eval": "(() => { const sidebar = document.querySelector('aside'); const themeBtn = [...document.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Tùy chỉnh giao diện'); if (!themeBtn) return 'no-btn'; const r = themeBtn.getBoundingClientRect(); const inSidebar = sidebar.contains(themeBtn); return JSON.stringify({ desktopThemeBtn: true, inSidebar, visible: r.width > 0, size: Math.round(r.width) + 'x' + Math.round(r.height) }); })()", "name": "desktop-btn" },
  { "eval": "(() => { const btn = [...document.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Tùy chỉnh giao diện'); btn.click(); return 'clicked'; })()" },
  { "wait": 900 },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); if (!dialog) return 'no-dialog'; const r = dialog.getBoundingClientRect(); const centered = Math.abs((r.x + r.width / 2) - window.innerWidth / 2) < 40; const swatches = dialog.querySelectorAll('button[aria-label^=\"Màu\"]').length; const sliders = dialog.querySelectorAll('input[type=range]').length; return JSON.stringify({ centered, w: Math.round(r.width), swatches, sliders }); })()", "name": "desktop-dialog" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const rose = [...dialog.querySelectorAll('button[aria-label]')].find(b => b.getAttribute('aria-label').includes('San hô')); rose.click(); const root = document.documentElement; return 'coral: ' + root.style.getPropertyValue('--primary').trim(); })()", "name": "pick-coral" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const light = [...dialog.querySelectorAll('button')].find(b => b.textContent.includes('Sáng')); light.click(); return 'light'; })()" },
  { "wait": 800 },
  { "eval": "(() => { return JSON.stringify({ mode: document.documentElement.getAttribute('data-theme'), bg: getComputedStyle(document.body).backgroundColor }); })()", "name": "desktop-light" },
  { "screenshot": "C:/Users/Darne/AppData/Local/Temp/opencode/shots/desktop-theme.png" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const close = [...dialog.querySelectorAll('button')].find(b => b.getAttribute('aria-label')?.includes('Đóng')); close.click(); return 'closed'; })()" },
  { "wait": 500 },
  { "eval": "(() => { return JSON.stringify({ dialogGone: !document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'), themeHeld: document.documentElement.getAttribute('data-theme') }); })()", "name": "after-close" },
  { "viewport": [390, 844, 3], "name": "phone" },
  { "eval": "location.href = 'http://localhost:5173/library'; 'r'", "name": "phone-reload" },
  { "wait": 20000 },
  { "eval": "(() => { const btn = [...document.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Tùy chỉnh giao diện'); if (!btn) return 'no-btn'; btn.click(); return 'phone-open'; })()" },
  { "wait": 900 },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); if (!dialog) return 'no-dialog'; const r = dialog.getBoundingClientRect(); const isBottom = r.y > 300; return JSON.stringify({ isBottomSheet: isBottom, swatches: dialog.querySelectorAll('button[aria-label^=\"Màu\"]').length }); })()", "name": "phone-sheet" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const close = [...dialog.querySelectorAll('button')].find(b => b.getAttribute('aria-label')?.includes('Đóng')); close.click(); return 'closed'; })()" }
]
