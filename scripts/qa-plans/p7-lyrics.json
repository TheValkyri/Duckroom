[
  { "navigate": "/library", "settle": 3500 },
  { "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; rows[0].querySelector('button').click(); return 'play'; })()" },
  { "wait": 1500 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()" },
  { "wait": 1200 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const lyr = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label') || '').includes('lời bài hát')); lyr.click(); return 'lyrics-open'; })()" },
  { "wait": 1000 },
  {
    "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const overlay = [...fs.querySelectorAll('div')].find(c => String(c.className).includes('absolute inset-0 w-full h-full')); if (!overlay) return 'overlay:MISSING'; const txt = overlay.textContent.trim().slice(0, 120); const allBtns = [...overlay.querySelectorAll('button')].map(b => b.getAttribute('aria-label') || 'no-label'); return JSON.stringify({ text: txt, buttons: allBtns }); })()",
    "name": "lyrics-content"
  }
]
