[
  { "viewport": [390, 844, 3] },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); if (!dialog) return 'no-dialog'; const hue = dialog.querySelectorAll('input[type=range]')[0]; const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; return (async () => { let frames = 0; let longTasks = 0; const obs = new PerformanceObserver((l) => { longTasks += l.getEntries().length; }); obs.observe({ entryTypes: ['longtask'] }); const start = performance.now(); const tick = () => { frames++; if (performance.now() - start < 1000) requestAnimationFrame(tick); }; requestAnimationFrame(tick); for (let h = 0; h <= 360; h += 3) { setter.call(hue, String(h)); hue.dispatchEvent(new Event('input', { bubbles: true })); await new Promise(r => setTimeout(r, 8)); } await new Promise(r => setTimeout(r, 150)); obs.disconnect(); return JSON.stringify({ fps: Math.round(frames), longTasks, events: 121 }); })(); })()", "name": "drag-fps-inline" }
]
