[
  { "viewport": [390, 844, 3] },
  { "eval": "location.href = 'http://localhost:5173/library'; 'r'", "name": "reload" },
  { "wait": 24000 },
  { "tapSelector": "main .group button", "settle": 2000 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()" },
  { "wait": 1400 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sleep = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('Hẹn')); return sleep ? 'sleep-btn: OK — ' + sleep.getAttribute('aria-label') : 'sleep-btn: MISSING'; })()", "name": "sleep-btn" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sleep = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('Hẹn')); sleep.click(); return 'menu-open'; })()" },
  { "wait": 500 },
  { "eval": "(() => { const menu = document.querySelector('[role=menu]'); if (!menu) return 'menu: MISSING'; const items = [...menu.querySelectorAll('button')].map(b => b.textContent.trim()); return JSON.stringify({ items }); })()", "name": "sleep-menu" },
  { "eval": "(() => { const menu = document.querySelector('[role=menu]'); const opt30 = [...menu.querySelectorAll('button')].find(b => b.textContent.includes('30 phút')); opt30.click(); return 'set-30'; })()" },
  { "wait": 600 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sleep = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('Hẹn')); return JSON.stringify({ ariaNow: sleep.getAttribute('aria-label'), badge: sleep.textContent.trim() }); })()", "name": "sleep-active" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const min = [...fs.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Thu nhỏ trình phát'); min.click(); return 'collapse'; })()" },
  { "wait": 900 },
  { "eval": "(() => { localStorage.setItem('duckroom.searchHistory.library', JSON.stringify(['mck', 'hvl'])); return 'history-planted'; })()" },
  { "eval": "(() => { const input = document.querySelector(\"input[type='search']\"); input.focus(); return 'focused'; })()" },
  { "wait": 700 },
  { "eval": "(() => { const box = document.querySelector('[role=listbox][aria-label=\"Tìm kiếm gần đây\"]'); if (!box) return 'history-box: MISSING'; const items = [...box.querySelectorAll('button')].map(b => b.textContent.trim()).filter(t => t && t !== 'Xóa hết'); return JSON.stringify({ items }); })()", "name": "history-box" },
  { "eval": "(() => { const box = document.querySelector('[role=listbox]'); const item = [...box.querySelectorAll('button')].find(b => b.textContent.trim() === 'mck'); item.click(); return 'picked-mck'; })()" },
  { "wait": 600 },
  { "eval": "(() => { const input = document.querySelector(\"input[type='search']\"); const visible = [...document.querySelectorAll('main .group')].filter(r => r.offsetHeight > 0).length; return JSON.stringify({ q: input.value, filteredRows: visible }); })()", "name": "history-picked-result" },
  { "screenshot": "C:/Users/Darne/AppData/Local/Temp/opencode/shots/qol-a.png" }
]
