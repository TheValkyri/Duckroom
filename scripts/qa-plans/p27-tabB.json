[
  { "navigate": "/library", "settle": 4500 },
  { "wait": 2000 },
  {
    "eval": "(() => { const dock = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); if (!dock) return 'tabB: no dock (no projection)'; const strips = dock.querySelectorAll('.h-0\\\\.5 div'); const strip = dock.querySelector('.h-0\\\\.5 div'); return JSON.stringify({ tabBTitle: dock.textContent.slice(0, 25), tabBProgress: strip ? strip.style.width : 'none' }); })()",
    "name": "tabB-mirror"
  }
]
