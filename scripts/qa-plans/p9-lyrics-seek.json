[
  { "navigate": "/library", "settle": 4000 },
  {
    "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; for (let i = 0; i < Math.min(rows.length, 20); i++) { rows[i].querySelector('button').click(); } return 'no-op'; })()",
    "name": "noop"
  },
  {
    "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; rows[5].querySelector('button').click(); return 'playing row 6 (Yêu Anh Giết Anh)'; })()",
    "name": "play-6"
  },
  { "wait": 1500 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()" },
  { "wait": 1200 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const lyr = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label') || '').includes('lời bài hát')); lyr.click(); return 'open'; })()" },
  { "wait": 1200 },
  {
    "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const overlay = [...fs.querySelectorAll('div')].find(c => String(c.className).includes('absolute inset-0 w-full h-full')); if (!overlay) return 'overlay:MISSING'; const lyricLines = [...overlay.querySelectorAll('button')].filter(b => !b.getAttribute('aria-label')); const sample = lyricLines[3]?.textContent?.slice(0, 50); const active = overlay.querySelector('.text-white.scale-\\\\31 02') || [...overlay.querySelectorAll('button')].find(b => String(b.className).includes('scale-[1.02]')); return JSON.stringify({ lineCount: lyricLines.length, sampleLine: sample, activeLine: active?.textContent?.slice(0, 50) }); })()",
    "name": "lyrics-lines"
  },
  {
    "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const overlay = [...fs.querySelectorAll('div')].find(c => String(c.className).includes('absolute inset-0 w-full h-full')); const line = [...overlay.querySelectorAll('button')].filter(b => !b.getAttribute('aria-label'))[8]; if (!line) return 'no-line-8'; line.click(); return 'seeked-to-line-8: ' + line.textContent.slice(0, 30); })()",
    "name": "lyrics-tap-seek"
  },
  { "wait": 800 },
  {
    "eval": "(() => { const strip = document.querySelector('footer .h-0\\\\.5 div'); return strip ? 'width-after-seek: ' + strip.style.width : 'no-strip'; })()",
    "name": "seek-progress"
  }
]
