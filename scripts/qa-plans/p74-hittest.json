[
  { "viewport": [390, 844, 3] },
  { "navigate": "/library", "settle": 25000 },
  { "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; if (!rows.length) return 'no-rows'; rows[5].querySelector('button').click(); return 'play6'; })()" },
  { "wait": 2000 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()" },
  { "wait": 1500 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('lời bài hát')).click(); return 'open'; })()" },
  { "wait": 1500 },
  { "eval": "(() => { const sheet = document.querySelector('[role=dialog][aria-label*=\"Lời bài hát\"]'); if (!sheet) return 'no-sheet'; const el = document.elementFromPoint(195, 400); const inSheet = sheet.contains(el); const el2 = document.elementFromPoint(195, 700); const inSheet2 = sheet.contains(el2); const handleVisible = !!document.elementFromPoint(195, 100); const sheetTopEl = document.elementFromPoint(195, 90); const headerStillVisible = !!document.querySelector('[aria-label=\"Thu nhỏ trình phát\"]'); return JSON.stringify({ midInSheet: inSheet, lowerInSheet: inSheet2, at90isSheet: sheet.contains(sheetTopEl), headerVisibleBehindSheet: headerStillVisible }); })()", "name": "hit-test" },
  { "eval": "(() => { const sheet = document.querySelector('[role=dialog][aria-label*=\"Lời bài hát\"]'); const r = sheet.getBoundingClientRect(); const parent = sheet.parentElement.getBoundingClientRect(); return JSON.stringify({ sheetTop: Math.round(r.top), parentTop: Math.round(parent.top), parentH: Math.round(parent.height), sheetH: Math.round(r.height), yTransform: getComputedStyle(sheet).transform }); })()", "name": "deep-geometry" }
]
