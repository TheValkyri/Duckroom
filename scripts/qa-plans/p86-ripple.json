[
  { "viewport": [390, 844, 3] },
  { "eval": "location.href = 'http://localhost:5173/library'; 'r'", "name": "reload" },
  { "wait": 22000 },
  { "eval": "(() => { const btn = [...document.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Tùy chỉnh giao diện'); btn.click(); return 'picker-open'; })()" },
  { "wait": 900 },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const light = [...dialog.querySelectorAll('button')].find(b => b.textContent.includes('Sáng')); light.click(); return 'ripple-started'; })()", "name": "click-light" },
  { "eval": "(() => { const layers = [...document.querySelectorAll('#duckroom-theme-ripple, [id=duckroom-theme-ripple]')]; if (!layers.length) return 'no-ripple-layer (applied instantly?)'; const layer = layers[0]; const style = getComputedStyle(layer.querySelector('div:nth-child(2)')); return JSON.stringify({ rippleLayer: true, mask: style.maskImage.slice(0, 60), edgeCount: layer.children.length }); })()", "name": "ripple-geometry" },
  { "measureFPS": 900, "name": "fps-DURING-ripple" },
  { "wait": 900 },
  { "eval": "(() => { return JSON.stringify({ mode: document.documentElement.getAttribute('data-theme'), rippleGone: !document.getElementById('duckroom-theme-ripple'), bg: getComputedStyle(document.body).backgroundColor }); })()", "name": "after-ripple" },
  { "screenshot": "C:/Users/Darne/AppData/Local/Temp/opencode/shots/light-after-ripple.png" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const dark = [...dialog.querySelectorAll('button')].find(b => b.textContent.includes('Tối')); dark.click(); return 'ripple-back'; })()", "name": "click-dark" },
  { "measureFPS": 900, "name": "fps-DURING-ripple-2" },
  { "wait": 900 },
  { "eval": "(() => { return JSON.stringify({ mode: document.documentElement.getAttribute('data-theme'), rippleGone2: !document.getElementById('duckroom-theme-ripple') }); })()", "name": "dark-after" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const violet = [...dialog.querySelectorAll('button[aria-label]')].find(b => b.getAttribute('aria-label').includes('Tím')); violet.click(); return 'morph-start'; })()" },
  { "wait": 400 },
  { "eval": "(() => { const p = document.documentElement.style.getPropertyValue('--primary').trim(); return 'morph-400ms: ' + p; })()", "name": "morph-mid" },
  { "wait": 400 },
  { "eval": "(() => { return JSON.stringify({ morph-final: document.documentElement.style.getPropertyValue('--primary').trim(), preset: JSON.parse(localStorage.getItem('duckroom.theme.v1')).preset }); })()", "name": "morph-final" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const hueSlider = dialog.querySelectorAll('input[type=range]')[0]; const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; const before = JSON.parse(localStorage.getItem('duckroom.theme.v1')).hue; setter.call(hueSlider, '200'); hueSlider.dispatchEvent(new Event('input', { bubbles: true })); return JSON.stringify({ before, primaryLive: document.documentElement.style.getPropertyValue('--primary').trim() }); })()", "name": "drag-live" },
  { "wait": 120 },
  { "eval": "(() => { const st = JSON.parse(localStorage.getItem('duckroom.theme.v1')); return JSON.stringify({ duringDrag_hue: st.hue, note: 'chưa persist là đúng — đang kéo' }); })()", "name": "not-persisted-yet" },
  { "eval": "(() => { const dialog = document.querySelector('[role=dialog][aria-label=\"Tùy chỉnh giao diện\"]'); const hueSlider = dialog.querySelectorAll('input[type=range]')[0]; hueSlider.dispatchEvent(new PointerEvent('pointerup', { bubbles: true })); return 'released'; })()" },
  { "wait": 600 },
  { "eval": "(() => { const st = JSON.parse(localStorage.getItem('duckroom.theme.v1')); return JSON.stringify({ afterRelease_hue: st.hue, persistedNow: st.hue === 200 }); })()", "name": "persist-on-release" }
]
