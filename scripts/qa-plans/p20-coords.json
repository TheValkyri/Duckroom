[
  { "viewport": [390, 844, 3], "navigate": "/library", "settle": 4500 },
  {
    "eval": "(() => { const btn = document.querySelectorAll('main .group')[0].querySelector('button'); const r = btn.getBoundingClientRect(); return JSON.stringify([Math.round(r.x + 30), Math.round(r.y + 15)]); })()",
    "name": "coords"
  }
]
