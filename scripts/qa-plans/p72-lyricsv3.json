[
  { "viewport": [390, 844, 3] },
  { "navigate": "/library", "settle": 25000 },
  { "eval": "(() => { const rows = [...document.querySelectorAll('main .group')]; if (!rows.length) return 'no-rows'; rows[5].querySelector('button').click(); return 'play6'; })()", "name": "play6" },
  { "wait": 2000 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()" },
  { "wait": 1400 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const cur = [...fs.querySelectorAll('p')].filter(p => { const c = String(p.className); return c.includes('text-center') && c.includes('text-\\[13px\\]'); }); return cur.length ? 'current-line found: ' + cur[0].textContent.slice(0, 25) : 'not-found-classes: ' + [...fs.querySelectorAll('p')].map(p => String(p.className).slice(0, 30)).slice(0, 5).join(' | '); })()", "name": "current-line" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const lyr = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('lời bài hát')); lyr.click(); return 'open'; })()" },
  { "wait": 1000 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sheet = [...fs.querySelectorAll('[role=dialog]')].find(d => (d.getAttribute('aria-label')||'').includes('Lời bài hát')); if (!sheet) return 'no-sheet'; const r = sheet.getBoundingClientRect(); const vh = window.innerHeight; return JSON.stringify({ top: Math.round(r.y), h: Math.round(r.height), nearFullscreen: r.y >= 60 && r.y <= 120 && r.height >= vh - 140 }); })()", "name": "sheet-pos" },
  { "screenshot": "C:/Users/Darne/AppData/Local/Temp/opencode/shots/lyrics-full-v3.png" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sheet = [...fs.querySelectorAll('[role=dialog]')].find(d => (d.getAttribute('aria-label')||'').includes('Lời bài hát')); const line = sheet.querySelectorAll('button[data-base]')[12]; line.click(); return 'seek-line-12'; })()" },
  { "wait": 1200 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const cur = [...fs.querySelectorAll('p')].filter(p => String(p.className).includes('text-center')); const line = cur.find(p => p.textContent.trim().length > 3); return line ? 'PLAYER-LINE after seek: ' + line.textContent.slice(0, 40) : 'none'; })()", "name": "line-after-seek" },
  { "measureFPS": 2000, "name": "lyrics-fullscreen-fps" }
]
