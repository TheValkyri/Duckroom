[
  { "viewport": [390, 844, 3] },
  { "navigate": "/library", "settle": 25000 },
  { "tapSelector": "main .group button", "settle": 2000 },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); if (!d) return 'no dock'; const btns = [...d.querySelectorAll('button')].map(b => b.getAttribute('aria-label')).filter(Boolean); return JSON.stringify({ miniButtons: btns, hasPrev: btns.includes('Bài trước'), hasNext: btns.includes('Bài sau'), balanced: btns.includes('Bài trước') && btns.includes('Bài sau') }); })()", "name": "mini-prev-next" },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); const prev = [...d.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Bài trước'); const r = prev.getBoundingClientRect(); return 'prev-size: ' + Math.round(r.width) + 'x' + Math.round(r.height); })()", "name": "prev-size" },
  { "eval": "(() => { const d = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); d.querySelector('[role=button]').click(); return 'expand'; })()", "name": "expand" },
  { "wait": 1400 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); if (!fs) return 'no fs'; const minBtn = [...fs.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Thu nhỏ trình phát'); const r = minBtn.getBoundingClientRect(); const lyrBtn = [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('lời bài hát')); const lyrR = lyrBtn.getBoundingClientRect(); return JSON.stringify({ minTop: Math.round(r.y), lyrTop: Math.round(lyrR.y), breathingOK: r.y >= 12 }); })()", "name": "header-spacing" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); [...fs.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('lời bài hát')).click(); return 'lyrics-open'; })()", "name": "open-lyrics" },
  { "wait": 1000 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sheet = [...fs.querySelectorAll('[role=dialog]')].find(d => (d.getAttribute('aria-label')||'').includes('Lời bài hát')); if (!sheet) return 'lyrics-sheet: MISSING'; const r = sheet.getBoundingClientRect(); const cs = getComputedStyle(sheet); const handle = sheet.querySelector('.h-1\\\\.5.w-10'); const noBlackSlab = cs.backgroundColor !== 'rgb(9, 9, 11)'; return JSON.stringify({ isBottomSheet: r.y > 250, roundedTop: cs.borderRadius, hasHandle: !!handle, semiTransparent: cs.backgroundColor.includes(','), h: Math.round(r.height) }); })()", "name": "lyrics-sheet" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const sheet = [...fs.querySelectorAll('[role=dialog]')].find(d => (d.getAttribute('aria-label')||'').includes('Lời bài hát')); const close = [...sheet.querySelectorAll('button')].find(b => b.getAttribute('aria-label')?.includes('Đóng lời')); close.click(); return 'closed'; })()", "name": "close-lyrics" },
  { "wait": 800 },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const mini = [...fs.querySelectorAll('p')].filter(p => p.className.includes('leading-6')); return mini.length ? 'current-lyric-line-on-player: OK' : 'current-line not rendered (maybe no lyrics for this track)'; })()", "name": "current-line" },
  { "measureFPS": 2000, "name": "fullscreen-fps" },
  { "eval": "(() => { const fs = [...document.querySelectorAll('div')].find(e => String(e.className).includes('fixed inset-0 z-50')); const min = [...fs.querySelectorAll('button')].find(b => b.getAttribute('aria-label') === 'Thu nhỏ trình phát'); min.click(); return 'collapsed'; })()", "name": "collapse" },
  { "wait": 900 },
  { "eval": "(() => { const chip = [...document.querySelectorAll('button')].find(b => (b.getAttribute('aria-label')||'').includes('Phát tiếp từ vị trí')); if (!chip) return 'no resume chip this session (OK — chỉ hiện khi restore)'; const r = chip.getBoundingClientRect(); const dock = [...document.querySelectorAll('footer')].find(f => f.getAttribute('aria-label') === 'Trình phát thu nhỏ'); const dr = dock.getBoundingClientRect(); return JSON.stringify({ chipY: Math.round(r.y), dockY: Math.round(dr.y), gap: Math.round(dr.y - r.bottom), noOverlap: r.bottom <= dr.y }); })()", "name": "resume-chip-position" }
]
