[
  { "viewport": [390, 844, 3] },
  { "navigate": "/library", "settle": 9000 },
  { "measureFPS": 2000, "name": "library-idle" },
  {
    "eval": "(async () => { const main = document.querySelector('main'); const t0 = performance.now(); let frames = 0; let longTasks = 0; const obs = new PerformanceObserver((l) => { longTasks += l.getEntries().length; }); obs.observe({ entryTypes: ['longtask'] }); await new Promise((res) => { const start = performance.now(); const step = () => { frames++; main.scrollTop += 90; if (main.scrollTop < main.scrollHeight - main.clientHeight && performance.now() - start < 2500) requestAnimationFrame(step); else res(); }; requestAnimationFrame(step); }); obs.disconnect(); return JSON.stringify({ scrollFps: Math.round(frames / ((performance.now() - t0) / 1000)), frames, longTasks, scrolledTo: Math.round(main.scrollTop), totalH: main.scrollHeight }); })()",
    "name": "library-scroll-test"
  },
  {
    "eval": "(async () => { const t0 = performance.now(); let longTasks = 0; const obs = new PerformanceObserver((l) => { longTasks += l.getEntries().length; }); obs.observe({ entryTypes: ['longtask'] }); const tab = [...document.querySelectorAll(\"nav[aria-label='Điều hướng chính'] a\")].find(a => a.getAttribute('href') === '/my-library'); tab.click(); await new Promise((r) => setTimeout(r, 2200)); const dur = performance.now() - t0; obs.disconnect(); const active = document.querySelector(\"nav[aria-label='Điều hướng chính'] a[aria-current='page']\"); return JSON.stringify({ navMs: Math.round(dur), longTasks, activeTab: active ? active.textContent.trim() : 'NONE' }); })()",
    "name": "nav-latency-test"
  },
  { "measureFPS": 2000, "name": "mylib-idle" },
  {
    "eval": "(async () => { const t0 = performance.now(); let longTasks = 0; const obs = new PerformanceObserver((l) => { longTasks += l.getEntries().length; }); obs.observe({ entryTypes: ['longtask'] }); const tab = [...document.querySelectorAll(\"nav[aria-label='Điều hướng chính'] a\")].find(a => a.getAttribute('href') === '/library'); tab.click(); await new Promise((r) => setTimeout(r, 2200)); const dur = performance.now() - t0; obs.disconnect(); const rows = document.querySelectorAll('main .group').length; return JSON.stringify({ backNavMs: Math.round(dur), longTasks, rows }); })()",
    "name": "nav-back-latency"
  },
  {
    "eval": "(async () => { const input = document.querySelector(\"input[type='search']\"); const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; let longTasks = 0; const obs = new PerformanceObserver((l) => { longTasks += l.getEntries().length; }); obs.observe({ entryTypes: ['longtask'] }); const t0 = performance.now(); for (const ch of ['m','c','k']) { setter.call(input, input.value + ch); input.dispatchEvent(new Event('input', { bubbles: true })); await new Promise((r) => setTimeout(r, 450)); } const dur = performance.now() - t0; obs.disconnect(); const rows = document.querySelectorAll('main .group').length; return JSON.stringify({ typingMs: Math.round(dur), longTasks, filteredRows: rows }); })()",
    "name": "search-typing-test"
  }
]
